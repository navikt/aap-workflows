name: test

on: 
  workflow_dispatch: 
  
jobs:
  call-workflow:
    uses: navikt/aap-workflows/.github/workflows/frontend.yml@main
  call-workflow-with-inputs:
    uses: navikt/aap-workflows/.github/workflows/frontend.yml@main
    with:
      navn: Dette er en verdi
  call-workflow-with-inputs-and-secret:
    uses: navikt/aap-workflows/.github/workflows/frontend.yml@main
    with:
      navn: Dette er en verdi
    secrets:
      hemmelig: ${{secrets.MY_SECRET_NAME}}
  
